# -*- mode: conf; -*-
###################################################################################
# plugstack.conf -- SPANK configuration file for the '<%= scope['slurm::clustername'] %>'
# cluster
###################################################################################
# Documentation: https://slurm.schedmd.com/spank.html
#
# SPANK provides a very generic interface for stackable plug-ins which may be
# used to dynamically modify the job launch code in Slurm. SPANK plugins may be
# built without access to Slurm source code. They need only be compiled against
# Slurm's spank.h header file, added to the SPANK config file plugstack.conf,
# and they will be loaded at runtime during the next job launch
#
# Resources:
# - https://github.com/grondo/slurm-spank-plugins
# - https://github.com/hautreux/slurm-spank-x11
# - https://github.com/edf-hpc/slurm-llnl-misc-plugins
#
#________________________
# Example of plugin build
# (typically on one compute node)
#
#      mkdir -p /usr/local/src/git
#      cd /usr/local/src/git
#      git clone https://github.com/hpc2n/spank-private-tmp.git
#      # Check the version of the plugin
#      grep -i Version spank-private-tmp/slurm-spank-private-tmpdir.spec
#      # ... and rename the directory accordingly
#      mv spank-private-tmp spank-private-tmp-0.0.2
#      tar cvf spank-private-tmp-0.0.2.tar.gz spank-private-tmp-0.0.2
#      # Create the RPM '/root/rpmbuild/RPMS/x86_64/spank-private-tmp-0.0.2-1.x86_64.rpm'
#      rpmbuild -ta spank-private-tmp-0.0.2.tar.gz
#
#      # Now you can install it:
#      rpm -i ~/rpmbuild/RPMS/x86_64/spank-private-tmp-0.0.2-1.x86_64.rpm
#      # OR
#      yum -y --nogpgcheck localinstall ~/rpmbuild/RPMS/x86_64/spank-private-tmp-0.0.2-1.x86_64.rpm
#
###################################################################################
# /!\ DO NOT EDIT THIS FILE: It has been automatically generated by Puppet.
#  In particular, any further changes will be overwritten at the next puppet
#  invocation
###################################################################################
#
include /etc/slurm/<%= scope['slurm::params::pluginsdir'] %>/*.conf
